# ============================================
# ChatExcel Docker 环境变量配置
# 复制此文件为 .env.docker 并根据实际情况修改
# ============================================

# -------- 端口配置 --------
FRONTEND_PORT=9012
BACKEND_PORT=9011

# -------- LLM 配置 --------
# 本地部署的 Qwen3 模型地址
# 如果 LLM 在宿主机运行，使用 host.docker.internal
LLM_BASE_URL=http://host.docker.internal:8067/v1
LLM_MODEL_NAME=Qwen3-30B-A3B-Instruct-2507
LLM_API_KEY=none
LLM_TEMPERATURE=0.4

# 或者使用 DeepSeek API
# LLM_BASE_URL=https://api.deepseek.com/v1
# LLM_MODEL_NAME=deepseek-chat
# LLM_API_KEY=your-deepseek-api-key

# -------- LLM 并发控制 --------
LLM_MAX_CONCURRENT=10

# -------- 数据库配置 --------
# 格式: mysql+aiomysql://用户名:密码@主机:端口/数据库名
# 注意: 密码中的特殊字符需要 URL 编码，如 @ 编码为 %40
# 在Docker容器中使用host.docker.internal访问宿主机
CHAT_HISTORY_DB_URL=mysql+aiomysql://root:LanSoftQ1w2e3%40@host.docker.internal:3305/nai

# -------- Redis 配置 --------
# 在Docker容器中使用host.docker.internal访问宿主机
REDIS_HOST=host.docker.internal
REDIS_PORT=6377
REDIS_PASSWORD=YKHCQ1w2e3!
REDIS_DB=5

